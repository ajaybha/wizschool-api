
model User {
  id       String     @id @default(auto()) @map("_id") @db.ObjectId
  address  String     @unique
  email    String?     
  name     String?

  // relation fields
  collection Collection[]
}